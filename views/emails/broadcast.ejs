<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= subject %>
    </title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            background: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        .header {
            padding: 30px 40px;
            text-align: center;
        }

        .header.announcement {
            background: linear-gradient(135deg, #3b82f6, #1d4ed8);
        }

        .header.maintenance {
            background: linear-gradient(135deg, #f59e0b, #d97706);
        }

        .header.urgent {
            background: linear-gradient(135deg, #ef4444, #dc2626);
        }

        .header h1 {
            color: white;
            margin: 0;
            font-size: 24px;
            font-weight: 600;
        }

        .header .icon {
            font-size: 48px;
            margin-bottom: 15px;
        }

        .content {
            padding: 40px;
        }

        .content h1,
        .content h2,
        .content h3 {
            color: #1a1a1a;
            margin-top: 0;
        }

        .content p {
            margin: 0 0 15px;
            color: #444;
        }

        .content a {
            color: #3b82f6;
        }

        .content img {
            max-width: 100%;
            height: auto;
            border-radius: 8px;
        }

        .footer {
            background: #f8f9fa;
            padding: 25px 40px;
            text-align: center;
            border-top: 1px solid #eee;
        }

        .footer p {
            margin: 0;
            color: #666;
            font-size: 13px;
        }

        .footer a {
            color: #3b82f6;
            text-decoration: none;
        }

        .badge {
            display: inline-block;
            padding: 6px 14px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 10px;
        }

        .badge.announcement {
            background: rgba(255, 255, 255, 0.2);
            color: white;
        }

        .badge.maintenance {
            background: rgba(255, 255, 255, 0.2);
            color: white;
        }

        .badge.urgent {
            background: rgba(255, 255, 255, 0.3);
            color: white;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header <%= emailType %>">
            <% if (emailType==='announcement' ) { %>
                <div class="badge announcement">📢 Announcement</div>
                <% } else if (emailType==='maintenance' ) { %>
                    <div class="badge maintenance">🔧 Maintenance Notice</div>
                    <% } else if (emailType==='urgent' ) { %>
                        <div class="badge urgent">⚠️ Urgent Notice</div>
                        <% } %>
                            <h1>
                                <%= subject %>
                            </h1>
        </div>

        <div class="content">
            <%- content %>
        </div>

        <div class="footer">
            <p>
                This email was sent to you because you have an account with us.<br>
                <a href="<%= siteUrl %>">Visit Dashboard</a>
            </p>
            <p style="margin-top: 10px; color: #999;">
                &copy; <%= new Date().getFullYear() %>
                    <%= siteName %>. All rights reserved.
            </p>
        </div>
    </div>
</body>

</html>